# シーケンサのタイムライン 使い勝手の作り直し 計画書

- 対象リポジトリ: `GameTemplate`（アプリ） / `KentoCompoEngine`（`engine/` サブモジュール）
- 作成日: 2026-09-14
- 目的: 初めて触った人が「トラックを足す → キーを打つ → 動きを整える」まで迷わずできるようにする。あわせてエディタの ImGui を日本語にする

---

## 1. ユーザーからの指摘と原因

| # | 指摘 | 原因 |
|---|---|---|
| 1 | キーの追加の仕方が分からない | キーを打つ方法がツールバーの「Add Key (K)」だけ。しかも「再生ヘッドの時刻」「選択中（無ければ先頭）のトラック」「役に実体が割り当て済み」の3つがそろわないと打てず、失敗はツールバーの小さな文字でしか出ない（`SequencerEditor.cpp` の `AddKeyAtCurrentTime`） |
| 2 | Add Track で下に行が増えたが、ホイールで下へ行けず、タイムラインが横に動く | キャンバスに縦スクロールが無く、はみ出した行は見えないまま。ホイールは時間の横送りに割り当てられている（`DrawTimelineWindow` のホイール処理） |
| 3 | 見方がよく分からない | 行は「トラック名 / チャンネル名」の文字だけで、カメラトラックは8行並ぶ。たためない・色分けが無い・キーの中身（値・補間）が見えない |
| 4 | ベジェを操作するカーブの画面が無い | 補間は Inspector の小さなグラフ（区間1つぶん）でしか触れない。値の変化を時間軸で見る画面が無い |

---

## 2. 作るもの

### 2.1 表示の動かし方（指摘 2）

- ルーラー（上）とトラック名欄（左）は固定し、行の部分だけを縦にスクロールできるようにする（`BeginChild` などで行の領域を分け、縦スクロールバーを出す）
- マウス操作を一般的な動画編集ソフトに合わせる
  - ホイール: 縦スクロール
  - Shift+ホイール: 時間の横スクロール
  - Ctrl+ホイール: 時間のズーム（今のまま、マウス位置の時刻を保つ）
  - 中ボタンドラッグ: つかんで移動（縦横）
- 時間方向の横スクロールバーを出す
- 「全体を表示」（キー F とボタン）: 全キーが収まるズームとスクロールにする
- ツールバーの `Zoom` / `Scroll` の数値欄は残してよいが、上の操作が主になる
- トラックを追加したら、その行が見える位置まで自動でスクロールする

### 2.2 キーの打ち方（指摘 1）

- **行をダブルクリック**: その行（チャンネル）の、その時刻（スナップ後）にキーを打つ。値は「その時刻のカーブの値」。キーが1つも無い行は、対象が割り当てられていればその今の値、無ければ型の既定値
  - `ICurveChannel` に `virtual bool AddKeyAt(float time)`（既定は偽）を足し、`CurveChannel<T>` で実装する。イベントの行は新しいイベントを置く
- **右クリックメニュー**（行の上）: 「ここにキーを打つ」「選択したキーを削除」「コピー」「ここに貼り付け」「補間: 一定 / 直線 / ベジェ」「イージング: プリセット」。キーの上で開いたら、そのキーを選んでから出す
- **貼り付け位置**: 右クリックメニューからの貼り付けは、クリックした時刻を先頭にする（Ctrl+V は今どおり再生ヘッド）
- **対象の割り当て忘れを見えるようにする**: 役に実体が割り当てられていないトラックは、見出しに注意の印と「対象が未割り当て」を出し、クリックでそのトラックを選んで Inspector の割り当て欄を出す
- 操作結果のメッセージ（キーを打てない理由など）は、タイムラインの上に数秒出して目立たせる
- 空のシーケンスのときは、キャンバスに「トラックを追加 → 対象を割り当て → 行をダブルクリックでキー」の手順を出す

### 2.3 見方（指摘 3）

- トラックごとに見出し行を置き、チャンネルの行はたためるようにする（▶/▼）。見出し行には、全チャンネルのキーをまとめて表示する（見出し行のキーを動かすと、その時刻のキーを全チャンネルまとめて動かす）
- トラックの種類ごとに見出しの色を変える（Camera / Transform / Light / PostProcess / Event / Screen / Text / Text3D / Component）
- キーの形を補間で変える: ベジェ = ひし形、直線 = 丸、一定 = 四角
- キーにマウスを乗せたら、時刻・値・補間をツールチップで出す
- 選択中の行をはっきり分かる色にする。ミュート中の行は全体を暗くする
- 再生ヘッドの時刻をルーラーの上に数字で出す

### 2.4 カーブエディタ（指摘 4）

- Sequencer ウィンドウの中に「ドープシート / カーブ」の切り替え（タブ）を置く。カーブ表示では、選択中の行（複数可）の値を 縦 = 値、横 = 時間 のグラフで描く
- 表示: キーを点、キーの間の補間を線で描く（評価した値を細かくサンプルする）。Vector3 / Vector4 は成分ごとに色を分けた線（x = 赤、y = 緑、z = 青、w = 灰）。回転（Quaternion）はオイラー角（度）の3本で見せる
- 操作:
  - キーの点をドラッグで時刻と値を動かす（Shift で片方だけ）
  - ベジェ補間の区間は、制御点のハンドルをグラフの上に出し、ドラッグで形を変える。今の `BezierHandle`（区間を 0〜1 に正規化した cubic-bezier）をそのまま使い、区間の実際の時刻と値に引き伸ばして描く。x は 0〜1 に制限する（単調でないと解が決まらないため）
  - ホイール = 値方向のズーム、Shift+ホイール = 時間方向、中ボタンドラッグ = 移動、F = 選択した行が全部収まるように合わせる
  - 右クリックメニューは 2.2 と同じ
- 編集は今と同じく `TrackEditCommand` を通し、ドラッグ1回を Undo 1回にする（掴んだときに編集前を取り、離したときに積む。今のキーの複数ドラッグと同じ形）
- 値を成分で読み書きするため、`ICurveChannel` に次を足す（既定は「成分なし」）
  - `size_t GetComponentCount() const`
  - `float GetKeyComponent(size_t key, size_t component) const` / `void SetKeyComponent(size_t key, size_t component, float value)`
  - `float EvaluateComponent(float time, size_t component) const`
  - Quaternion は成分をオイラー角（度）として扱い、書くときにクォータニオンへ戻す
  - イベントとテキストの行は成分なし（カーブ表示に出さない）
- Inspector の小さなベジェのグラフは残す（キー1つを詰める用）

### 2.5 日本語化

- 対象: エンジンのエディタ UI（Sequencer、Inspector の各欄、Hierarchy、Settings の各ページ、メニュー、ツールチップ、状態メッセージ）
- 用語は英語かカタカナのままにする。例: トラック、キー、カーブ、ベジェ、Transform、Component、FOV、Bloom、Aim、プレハブ、Undo
- ウィンドウ名とタブ名は、表示だけ日本語にして ID は英語のままにする（`"シーケンサ###Sequencer"` のように `###` を使う）。imgui.ini のレイアウトと、`MyGame.cpp` のドッキング設定を壊さないため
- Settings のページ名を変えると、保存している「選んでいたページ」が一度外れるが、それは許す
- 日本語のフォントはすでに読み込まれている（Inspector の案内文が日本語で出ている）。出ない字があれば、読み込んでいる文字の範囲を確認する

---

## 3. 進め方

- 作業ブランチ: `feature/sequencer-ui`（engine と親の両方）。`git worktree add` で作る（場所は Codex の書き込みが許されている所。リポジトリの中に作るなら `.worktrees/` 以下にして、`.git/info/exclude` に `.worktrees/` を足す）。`engine/externals/DirectXTex/Shaders/Compiled/*.inc`（14個）をコピーしてからビルドする
- 2.1 → 2.2 → 2.3 → 2.4 → 2.5 の順に、止まらずに全部やる。区切りごとに Debug|x64 でビルド（終了コード 0）→ engine → 親の順でコミット。最後に Release|x64 もビルドする
- 見た目と操作感は自動では確かめられないので、区切りごとに「何をどう操作すると確かめられるか」をコミットメッセージか報告にまとめる
- `CollisionManager` と `gameobject/` 以下は触らない
