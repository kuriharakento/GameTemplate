# 演出シーケンサ／カットシーンエディタ 計画書

- 対象リポジトリ: `GameTemplate`（アプリ） / `KentoCompoEngine`（`engine/` サブモジュール）
- 作成日: 2026-09-10
- 目的: 楽曲同期のライブ演出と、ゲーム本編のカットシーン演出の**両方**を作成できるエディタを構築する

---

## 1. 目的とスコープ

### 1.1 目的

タイムライン上でカメラ・ライト・キャラクター・エフェクト・音を編集し、JSONとして保存・再生できる演出エディタを作る。

参照する表現目標はアイドルマスターシャイニーカラーズ Song for Prism のライブ映像だが、**ライブ専用ツールにはしない**。同じ基盤でゲーム本編のカットシーン（会話、必殺技演出、イベントシーン）を作れることを要件とする。

### 1.2 ライブとカットシーンの要求の違い

この違いが設計の根幹を決める。

| | ライブ | カットシーン |
|---|---|---|
| 進行 | 一本道 | 分岐・条件あり |
| 画面 | 全画面・自己完結 | ゲームUIと共存 |
| 状態 | シーン専用 | **ゲーム状態と地続き** |
| 中断 | 基本なし | **スキップ・早送りが必須** |
| 再利用 | 曲ごとに固有 | **部品として使い回す** |

右列の要求（特にスキップと再利用）が、データモデルに強い制約を課す。詳細は 3 章。

### 1.3 スコープ外（当面）

- 動画テクスチャ再生（Media Foundation の映像ストリーム対応）
- ラグドール、デカール、天候システム
- ネットワーク同期

---

## 2. 現状分析

### 2.1 すでにある資産

| 領域 | 実装 |
|---|---|
| 描画 | DirectX12、Deferred（G-Buffer + LightPass）+ Forward 併用 |
| 影 | カスケードシャドウ4枚、スポット／ポイントライト個別シャドウ |
| キャラ | スキニング（Compute Shader）、`Animator`（`SetCurrentTime` によるシーク可） |
| カメラ | `CameraManager`、`Camera`（FOV／シェイク／ズーム）、`SplineCamera`（CatmullRom） |
| ライト | Directional / Point / Spot（各100本まで）、シャドウ対応 |
| エフェクト | モジュール式パーティクル、GPUパーティクル、`ParticleEditor` |
| ポスト | Bloom / Grayscale / Vignette / Noise / CRT |
| 音 | XAudio2 + Media Foundation（mp3/m4a/mp4 対応済）、グループ・フェード・リバーブ |
| シリアライズ | nlohmann/json、`JsonEditableBase` |
| **エディタ外枠** | **ImGui Docking有効、Hierarchy / Inspector / Console / Project の4ペイン、シーンのRenderTexture表示** |

最後の項目が重要で、**エディタシェルは既に存在する**。本計画は「エディタを一から作る」のではなく「既存シェルにタイムラインを追加する」作業になる。

### 2.2 決定的に欠けているもの

**時間軸を一元管理し、任意時刻を評価する層が存在しない。** 本計画の中核はこれを作ることである。

---

## 3. データモデル設計（最重要）

ここで決めた事項は後から変更するとデータが全て無効になる。**実装量は小さいが、最初に決めきる。**

### 3.1 `Evaluate(t)` の純関数契約

全トラックに以下を強制する。

```cpp
virtual void Evaluate(float time, const BindingContext& ctx) = 0;
```

**「毎フレーム加算して進める」実装を禁止する。** これを守ると、以下が同時に成立する。

- エディタのスクラブ（タイムラインを掴んで前後に動かす）
- カットシーンのスキップ（`t = end` を一発適用するだけで最終状態になる）
- 早送り・巻き戻し
- 再生位置からの途中再生

逆に副作用の積み上げで実装すると、**スキップが原理的に実装不可能**になる。

例外はパーティクルなど本質的に状態を持つもの。これは「クリップ先頭から上限付きで早送りシミュレート」＋「乱数シード固定」で近似し、完全な逆再生は仕様として諦める。

### 3.2 トラックバインディング

シーケンスはオブジェクトを直接指さず、**役（ロール）**を指す。実体は再生時に割り当てる。

```
Sequence "被弾リアクション"
└─ CharacterTrack : binding = "Target"    ← 中身は空
   CameraTrack    : binding = "MainCam"

Play(seq, { {"Target", enemyA}, {"MainCam", playerCam} });
```

これが無いと「攻撃演出」「会話」「ドア開閉」を部品化できず、ライブ専用ツールになる。

### 3.3 安定ID（GUID）

現状 `GameObjectManager::Find(name)` の名前引きしかない。演出データが名前で参照すると改名・重複で壊れる。全 `GameObject` に GUID を持たせ、参照はGUIDで行い、名前は表示専用とする。

アセット（テクスチャ、モデル、音声）も同様。`TextureManager` / `ModelManager` はファイルパス文字列がキーであり、**直近のコミットで実際にリソースフォルダを移動している**ため、この問題は既に一度踏んでいる。

### 3.4 スキーマバージョン

全JSONの先頭に `"version": 1` を入れる。**コストはほぼゼロだが、これが無いとフォーマット変更時に既存の演出データが全て読めなくなる。** 演出データは何十時間もかけて作る資産であり、最も避けるべき事態。

### 3.5 状態の退避／復元

シーケンス開始時に対象オブジェクトの状態を保存し、終了時に復元する。**エディタのスクラブにも同じ仕組みが必要**なので、二重に重要。

### 3.6 データ構造

```
Sequence (json)
├─ meta      : version, duration, bpm, offset, audioClip
├─ Binding[] : role名 → 型（Character / Camera / GameObject / Light）
├─ Track[]
│   ├─ CameraTrack      : Clip[Static / Spline / Orbit / Handheld / Follow]
│   ├─ CharacterTrack   : Clip[animClip, blendIn/Out, speed]
│   ├─ FaceTrack        : Curve[morphWeight]        ← 表情・リップシンク
│   ├─ TransformTrack   : Curve<Vector3> pos/rot/scale
│   ├─ LightTrack       : Curve[color, intensity, coneAngle, beamDensity]
│   ├─ EffectTrack      : Clip[emitterName]
│   ├─ PostProcessTrack : Curve[bloom, vignette, dof, lutBlend]
│   ├─ MaterialTrack    : Curve[dissolve, emissive, uvScroll, alpha]
│   ├─ ScreenTrack      : Clip[source切替, UVズーム, 輝度]
│   ├─ SpriteTrack      : Clip[texture, α]           ← 歌詞・UI
│   ├─ AudioTrack       : Event[SE / Voice]
│   ├─ TimeScaleTrack   : Curve[timeScale]           ← スロー演出
│   ├─ ActivationTrack  : Clip[表示 / 非表示]
│   ├─ SubSequenceTrack : Clip[別Sequence]           ← ネスト
│   └─ EventTrack       : Event[name] → コールバック
└─ Marker[] : サビ / Aメロ / ビート
```

### 3.7 カーブとイージング

現状 `ParticleEasing.h` は `EasingType` のプリセット列挙のみで、**任意ハンドルのベジェが無い**。演出の質はここでほぼ決まるため最優先で作る。

- `Curve<T>` に統一。キーごとに CSS風 `cubic_bezier(x1,y1,x2,y2)` を持つ
- x が単調なのでニュートン法3〜4反復で t を解けば十分な精度
- 既存 `EasingType` は廃止せずプリセットとして残し、**内部でベジェ4係数に変換して評価経路を1本化**する（`EaseInOutCubic` ≒ `(0.65, 0, 0.35, 1)`）。既存のパーティクルや `CPUSpotLight::easingFunction` と互換が保てる
- 型別の補間: `float` / `Vector3` は成分ごと、`Quaternion` は Slerp、色は必要なら OKLab
- **パスと速度を分離する**。空間パス（CatmullRom / ベジェ）上の進行度 `s(t)` にイージングを掛ける。等速で走るスプラインカメラは安っぽくなる

### 3.8 時間の権威

`TimeManager` の deltaTime 積算では数分の楽曲で必ずズレる。**オーディオの再生位置を時間の権威とする。**

`IXAudio2SourceVoice::GetState()` の `SamplesPlayed` から秒を算出する `GetPlayPosition()` と、PCM全展開済みという特性を活かした `Seek()`（`SubmitSourceBuffer` の `PlayBegin` を使用）を `Audio` に追加する。**現状これらのAPIが存在しないことが最大の障害。**

---

## 4. 描画パイプラインの整備

エディタ着手前に手を入れるべき。理由は「今後パスを大量に追加するため」。

### 4.1 メインRTがLDR ← 単独で最優先

`framework/Framework.cpp:153` でメインRTが `DXGI_FORMAT_R8G8B8A8_UNORM_SRGB` になっている。一方ブライトパスとブラー用だけが `R16G16B16A16_FLOAT`。

```
ライトパス出力 → [LDR RT] ← ここで 1.0 にクリップ
                    ↓
              ブライトパス → [HDR RT]  ← もう手遅れ
```

**ステージ照明の強い発光やビームが、抽出される前に潰れる。** ブルーム閾値を上げても白飽和した面しか出ないのはこれが原因。またリニア空間の計算結果をsRGBフォーマットのRTに書いた後にポストプロセスしており、ガンマの扱いも要確認。

対処: メインRTを `R16G16B16A16_FLOAT` にし、ポストプロセス最終段にトーンマップ（ACES等）を追加する。**照明系の演出機能はすべてこれが前提。**

### 4.2 描画順がアプリ側にベタ書き

`application/MyGame.cpp:123-330` に、シャドウ3種 → G-Buffer → ライトパス → フォワード → ポストプロセス → 2D → ImGui が約200行の直列コードで展開され、ルートパラメータ番号（10〜15）まで手書きされている。さらに `#ifdef USE_IMGUI` で最終出力先が分岐しており、**パスを足すたび両経路を直す**必要がある。

今後ここにボリュメトリック、DoF、TAA、半透明パス、モニター用サブビュー、トランジション、レターボックスを追加すると確実に破綻する。

対処: エンジン側に `RenderPipeline` を作り、パスを差し替え可能な列として持つ。**フルスペックのRenderGraph（自動バリア解決・リソースエイリアシング）は過剰**で、パスの並びと入出力RTの受け渡しを整理するだけで十分。

### 4.3 「ビュー」の概念が無い

`renderTexture_`、`deferredRenderer_`、`GBuffer` がいずれも単一インスタンス固定で、**任意のカメラで任意のRTにシーンを描く手段が無い**。

これが無いと以下が全て実現できない。

- ステージモニターへの中継映像（サブカメラ → RT）
- エディタのカメラプレビュー小窓
- 床の平面反射
- ミニマップ、キャラクタープレビュー

`View { Camera*, GBuffer, RenderTexture, 描画対象フィルタ }` として切り出す。**後から入れると各パスの署名が全部変わるため手戻りが大きい。**

### 4.4 半透明パスが無い

フォワードは `Draw3D` → デバッグライン → Skybox → パーティクルの固定順で、半透明のソートが無く**パーティクルは常に最後**。ビーム・煙・ガラスが同居する演出で破綻する。

不透明パスと半透明パス（深度書き込みOFF、奥から手前ソート）を分離する。Skyboxは不透明の後・半透明の前。

### 4.5 その他

- **シャドウパスがライト数ぶんシーン全体を再描画**（ポイントライトは6面）。ライブでライトを増やすと破綻する。上限設定・更新頻度の間引き・キャスタのカリングが要る。ただし**性能問題が出てから対処で間に合う**
- **バリア管理が手動**（`TransitionDepthToDepthWrite` / `TransitionDepthToSRV` をアプリから呼んでいる）。`RenderPipeline` 側で入出力を宣言させ自動化するのが本筋

---

## 5. エディタ基盤

### 5.1 後付けが最も高くつく層

| 項目 | 内容 |
|---|---|
| **Undo/Redo のコマンド基盤** | 既存の `JsonEditor` / `GameObjectEditor` は値を直接書き換える作り。タイムラインだけ対応しても一貫性の無いエディタになる。**全編集をコマンド経由にする規約を先に決める** |
| **選択状態の一元管理** | Hierarchy・Inspector・タイムライン・ギズモが同じ「選択中」を共有する。各UIが個別に持つと必ず破綻する |
| **ImGuizmo の導入** | `externals/` に無い。ビューポート上でカメラキーを掴めないエディタは実用にならない |
| **入力競合の整理** | `Input` シングルトンが `io.WantCaptureKeyboard` を見ていないと、テキスト入力中にゲームが動く。Ctrl+Z / Space を割り当てる前に整理する |
| **編集モードと再生モードの分離** | 現在は常時ゲームが動いている。「編集中はゲーム時間を止め、シーケンサの時刻だけで絵を作る」状態が必要。`TimeManager::Pause()` はあるので接続の問題 |

### 5.2 先にやると後が速くなるもの

- **シェーダのホットリロード** — `DirectXCommon` が `IDxcCompiler3` を実行時に保持しているため実装可能。これから描画パスを大量に足す作業で、シェーダ1行のためにアプリを再起動するのは効率が悪すぎる。投資回収が早い

### 5.3 後回しでよいもの

- GPUタイムスタンプ／PIXマーカー（パスが増えてから）
- 保存の安全化（テンポラリ書き→リネーム）
- タイムラインのトラックグループ、スナップ、コピペ、複数選択

### 5.4 小さいが早めに片付けたいもの

- `imgui.ini` がリポジトリにコミットされている（ルートと `application/` の両方）。エディタ開発中はウィンドウ配置を頻繁に触るため、**毎回差分が出る**。`.gitignore` に入れるか、デフォルトレイアウトをコードで持つかを決める
- `MyGame.cpp` のエディタUI（メインメニューバー等）をエンジン側へ移す。ここに演出エディタを足すとさらに膨らむ

---

## 6. 決断が必要な事項

### 6.1 オブジェクトモデルの二重化 ← 最優先の決断

`engine/ecs/` に完全なECSが実装されている（`Registry`、`EntityBuilder`、`EntityView`、11コンポーネント、5システム、`EcsInspector`）。一方 `application/` では**ECSの利用箇所がゼロ**で、`GameObject` 系だけで動いている。

シーケンサのバインディング先をどちらかに決める必要がある。両対応は複雑さが倍になり、後から乗り換えると演出データが全て無効になる。

**推奨: `GameObject` 側に寄せる。** JSON対応、`GameObjectEditor`、コンポーネントファクトリ、アプリでの実績が全てそちらにある。ECSは群衆・ペンライト・大量の弾など「数で押す」用途に限定するか、当面凍結する。

### 6.2 エンジンとアプリの線引き

`engine/` はサブモジュールであり二重コミットが必要。今回の作業はエンジン側に大量の変更が入るため、線引きを先に決める。

- **エンジン**: シーケンサ基盤、カーブ、トラック種別、描画パス、エディタUI基盤
- **アプリ**: ライブ固有のもの（ペンライト、観客、ステージギミック）、ゲーム固有のイベント定義

### 6.3 その他の構造的課題

- **シングルトン19個の状態リセット** — `Framework::Finalize()` に解放順序が手書きで並んでおり「起動〜終了で1回」の前提。エディタはシーン再読込と再生モードの出入りを繰り返すため、各シングルトンに `Reset()` 相当が必要になる可能性が高い
- **エラー処理が assert 中心**（`assert` 159箇所に対し例外／`catch` は12箇所）。エディタでは「壊れたJSONを開く」「消したファイルを参照する」が日常的に起きる。**assertで落ちるエディタは使えない**。アセット読み込みとJSON読み込みの経路だけは失敗を戻り値にし、`ConsoleLog` に出す
- **非同期ロードが無い**（`std::thread` は `CollisionSystem` と `ConsoleLog` のみ）。エディタでのアセット差し替え時のフリーズと、ランタイムのヒッチが同じ原因
- `.vcxproj` にファイル106個が手書きされ `UpdateFilters.py` を別途実行する運用。シーケンサだけで数十ファイル増える

---

## 7. 演出機能の欠落一覧

基盤が正しければ**いつ足しても既存データが無駄にならない**もの。優先度は低い。

### 7.1 キャラクター表現

| 項目 | 状況 |
|---|---|
| **モーフターゲット（ブレンドシェイプ）** | `GraphicsTypes.h` に該当構造体が無い。**表情もリップシンクも不可能**。`assimp` があるので glTF の morph targets 自体は読める。`Skinning.CS.hlsl` の前段にモーフ加算を挟む |
| **トゥーン／NPRシェーディング** | `Object3d.PS.hlsl` は Half-Lambert + Blinn-Phong。ランプテクスチャ、リムライト、ハイライト制御が必要。**NPRでは物理的に正しいライティングだと肌が汚くなる**ため、「ライトの影響率」をマテリアルで制御する仕組みが要る。**照明演出のパラメータを詰める前に決めるべき** |
| アウトライン | 無し。背面法（inverted hull） |
| **揺れもの（髪・スカートのボーン物理）** | 無し。ダンス中に髪と衣装が硬直する。**実装量に対する見栄えの向上が最大** |
| 視線制御 | 無し。カメラ目線が作れない |
| IK | 無し。モーションが正なら優先度低 |

### 7.2 画づくり

| 項目 | 状況 |
|---|---|
| **トーンマッピング／LUTグレーディング** | 4.1 参照。曲調に合わせたルック切替はタイムライン向き |
| アンチエイリアス | MSAA / FXAA / TAA いずれも無し。トラスや細いビームがジャギる |
| **大気フォグ／煙** | 無し。**ボリュメトリックビームは空気中の粒子が前提なので、ビームより先に必要** |
| 床反射 | 無し。ライブ画の情報量のかなりの割合を占める。平面反射なら安い |
| SSAO／コンタクトシャドウ | 無し。足の接地感が出ない |
| モーションブラー | 無し。カメラの速い振りで破綻 |
| アナモルフィック／星型グレア | Bloomはガウスのみ |
| DoF | 無し |

### 7.3 ボリュメトリックライト

素材は揃っている（`GPUSpotLight` にコーン角・シャドウフラグ、スポットライト個別シャドウマップ、`GBuffer::GetDepthSRVIndex()` による深度SRV）。**無いのは散乱パスのみ。**

- **(A) スクリーンスペース・レイマーチ** — 深度からワールド位置を復元、カメラレイを32〜64ステップ行進、コーン内判定＋シャドウマップ比較で遮蔽、Henyey-Greenstein位相関数。1/2解像度＋ブルーノイズディザ＋バイラテラルアップサンプル。`volumetricEnabled` を持てるのは4〜8本まで
- **(B) コーンメッシュの加算合成** — 深度フェードでソフトエッジ。**「何十本ものビームが客席を舐める」絵はコストの都合でこちらでないと本数が出せない**

**推奨: B を先に入れて絵を作り、サビの見せ場だけ A を足す。** ただし 7.2 の大気フォグが先。

### 7.4 カメラワーク

`Camera` は translate / **オイラー角** rotate / FOV / シェイク / ズームのみ。

- **Quaternion化** — オイラー角のままカットをまたぐ大きな回転を補間すると確実に破綻する
- **LookAt制約**（ターゲット＝オブジェクト／ボーン、weight・追従ラグ付き）。位置だけ打てば向きが決まる。編集効率上ほぼ必須。追従ラグで「カメラマンが追いかけている感」が出る
- **手持ちノイズ** — Perlinノイズを pos / rot / FOV に別周波数・別振幅で。既存 `StartShake` は減衰する衝撃用で別物
- ドリーズーム、FOVブリージング

### 7.5 モニター／スクリーン映像

`RenderTexture` はSRVインデックスまで取得できるが、モデルのテクスチャは `TextureManager` のファイルパス管理であり、**RenderTextureを差し込む口が無い**。`Object3d::SetOverrideTextureSRV(uint32_t)` の追加が必要。

- **(a) ライブ中継カメラ** — サブカメラでRTに描画。シーンの二度描きなので、解像度を落とす／描画対象をキャラだけに絞る／30fps更新で抑える
- **(b) 事前収録VJ映像** — 動画テクスチャ。単体で1機能分の重さがあるためスコープ外。当面は連番テクスチャで代替
- **(c) 手続き生成VJ** — シェーダで波形・幾何模様。一番安く、一番映える

LEDらしさは専用シェーダのピクセルグリッド・スキャンライン・輝度ブーストで出す。既存 `CRTEffect` のロジックが流用可能。

### 7.6 カットシーン固有

| 項目 | 状況 |
|---|---|
| **会話・テキストシステム** | ダイアログ枠、話者名、タイプライタ送り、選択肢 |
| **日本語テキスト** | `FontSprite` が `DrawChar(char)` のASCIIビットマップフォント。**歌詞テロップも会話も出せない**。MSDFフォントかDirectWriteでのアトラス生成が必要 |
| **画面トランジション** | `SceneManager` は `ChangeScene` があるだけで演出付き遷移が無い。フェード／ワイプ／ディゾルブ |
| レターボックス | シネスコバー、アスペクト変更。実装が軽い割にカットシーンらしさが即出る |
| **3Dオーディオ・ボイス** | X3DAudio 未使用で**定位が無い**。BGMダッキングも無い。`SoundGroup::Voice` の枠はある |
| タイムスケール演出 | `TimeManager` に `timeScale` と `StartHitStop` が既にある。**シーケンサ自身は実時間で進む**分離が必要 |

### 7.7 ゲーム本編との接続

| 項目 | 状況 |
|---|---|
| **スキップ／早送り** | 3.1 の純関数契約が守られていれば `t = end` で実装できる |
| **ゲームプレイとの繋ぎ** | プレイヤーカメラ → カットシーンカメラのブレンド、現在地を原点とする相対座標シーケンス |
| **入力ロックと制御の返還** | 演出中のプレイヤー操作剥奪 |
| **条件分岐・イベント発火** | EventTrack からゲーム側コールバック |
| QTE／入力待ち | シーケンサが「待つ」状態を持てる必要がある |
| ローカライズ | テキストの外部化 |
| 非同期アセット先読み | 6.3 と同じ問題 |

### 7.8 ステージ・客席

- **観客とペンライトの群衆** — `InstancedModelRenderer` があるので土台は有り。色ウェーブ、位相のズレ、コール同期は専用システム
- **ステージギミック** — リフト、ムービングライト、紙吹雪、レーザー

---

## 8. 実行計画

### 8.1 Phase 0: 縦切り1本を通す ← 最初にやる

> **「楽曲を再生しながら、ベジェカーブで加減速するカメラを1カット分、ギズモで置いて、Undoできて、JSONに保存できる」**

トラックはカメラ1種類のみ。ライトもキャラもエフェクトも無し。

必要な前提作業:

- `Curve<T>` とベジェ評価（3.7）
- `Audio` の再生位置取得とシーク（3.8）
- Undo/Redo コマンド基盤（5.1）
- ImGuizmo 導入（5.1）
- 選択の一元管理（5.1）
- 編集／再生モードの分離（5.1）
- タイムラインUI（`ImDrawList` 自作、または `ImGuizmo` 同梱の `ImSequencer` / `ImCurveEdit`）

**この1本で、以下が全て同時に検証される。**

- `Evaluate(t)` の純関数設計は成立するか（スクラブすればわかる）
- 音とのフレーム同期は取れるか
- Undo/Redo のコマンド粒度は妥当か
- 選択とギズモの連携は使いやすいか
- JSONのスキーマ設計は破綻しないか

**設計の誤りは、傷が浅いうちに発見して作り直す。** 基盤を全部固めてから縦に通すと、間違いが判明したときの手戻りが最大化する。

### 8.2 その後の順序

| Phase | 内容 | 根拠 |
|---|---|---|
| 1 | **メインRTのHDR化＋トーンマップ** | 単独で絵が変わる。照明演出すべての前提。1〜2時間 |
| 2 | **シェーダのホットリロード** | 以降の全パス追加作業が速くなる |
| 3 | **`RenderPipeline` へのパス抽出** | 以降のパス追加コストが下がる。`#ifdef` 分岐も吸収 |
| 4 | **`View` の分離** | モニター映像・カメラプレビュー・反射がまとめて解決。後回しにすると手戻り大 |
| 5 | **半透明パスの分離とソート** | ビーム／煙を入れる前に必要 |
| 6 | **トラック拡充** — Transform / Light / PostProcess | カーブ編集の共通化が主作業 |
| 7 | **NPRシェーディング方針の決定と実装** | **照明演出のパラメータを詰める前**にやる |
| 8 | **大気フォグ → コーンメッシュ・ビーム(B)** | 順序が逆だと嘘くさくなる |
| 9 | **揺れもの（ボーン物理）** | 独立性が高く、見栄えの向上が最大 |
| 10 | **CharacterTrack（アニメブレンド）／EffectTrack** | |
| 11 | **ScreenTrack ＋ サブカメラRT** | Phase 4 に依存 |
| 12 | **日本語フォント → 歌詞・会話システム** | |
| 13 | **カットシーン接続**（スキップ、カメラ引き継ぎ、入力ロック、EventTrack） | |
| 14 | **モーフターゲット（表情・リップシンク）** | 影響は大きいが独立性が高く、他を止めない |
| 15 | レイマーチ・ボリュメトリック(A)、DoF、TAA、床反射 | 質の詰め |

### 8.3 やらないと決めること

以下は判断を保留せず、明示的に「やらない」と決めて記録する。

- 動画テクスチャ再生
- フルスペックのRenderGraph（自動バリア解決・リソースエイリアシング）
- IK、ラグドール、デカール
- ECSへの移行（6.1 の決断次第）

---

## 9. 着手前チェックリスト

Phase 0 の開始前に決定・記録した事項（2026-09-10 決定）。

- [x] **オブジェクトモデル（6.1）** — `GameObject` に寄せる。`engine/ecs/` は削除せず当面凍結する
- [x] **エンジンとアプリの線引き（6.2）** — 計画通り。シーケンサ基盤・カーブ・トラック種別・描画パス・エディタUI基盤はエンジン、ライブ／ゲーム固有のものはアプリ
- [x] **JSONの `"version"`（3.4）** — `engine/core/SchemaVersion.h` にドメインごとの定数を置く。エンジンが書き出す全JSONの先頭に付与し、バージョンを持たないデータは「バージョン1相当の旧データ」として扱う。未来のバージョンは読み込みを拒否する
- [x] **安定ID（3.3）** — 128bitのランダム値。`GameObject` がコンストラクタで自動採番し、JSONのロード時のみ保存値で上書きする。文字列表現は32文字のハイフン無し小文字16進。参照は `GameObjectManager::FindByGuid()`
- [x] **Undo/Redo のコマンド粒度（5.1）** — 「ユーザーの1操作 ＝ 1コマンド」。ドラッグは毎フレーム発行して `ICommand::MergeWith()` で1件に統合する。複数対象への一括操作は `CommandHistory` のトランザクションでまとめる。選択の変更は編集ではないため履歴に積まない
- [x] **`Evaluate(t)` の純関数契約（3.1）** — `engine/sequencer/core/ITrack.h` のクラスコメントに明文化した
- [x] **`imgui.ini`（5.4）** — `.gitignore` に入れて追跡から外した。初期レイアウトは `MyGame.cpp` の DockBuilder がコードで構築する

---

## 10. 実装状況

| Phase | 内容 | 状況 |
|---|---|---|
| 0 | 縦切り1本（Curve／純関数契約／バインディング／GUID／Audioの再生位置とシーク／Undo基盤／選択の一元管理／編集・再生モード分離／ImGuizmo／タイムラインUI／JSON保存） | 実装済み |
| 1 | メインRTのHDR化＋トーンマップ | 実装済み |
| 2 | シェーダのホットリロード | 実装済み（対象は PostProcess のみ。他パスは順次登録する） |
| 3 | `RenderPipeline` へのパス抽出 | 実装済み |
| 4 | `View` の分離 | 実装済み（サブビューを使う機能はまだ無い） |
| 5 | 半透明パスの分離とソート | 実装済み（Codex） |
| — | ビューごとの定数バッファ（Phase 4 の不具合修正、TASK-06/07） | 実装済み（Codex＋Claude） |
| 6 | トラック拡充 — Transform / Light / PostProcess ＋ カーブ編集の共通化 | 実装済み |
| 7 | NPRシェーディング方針の決定と実装 | 実装済み（暫定方針：アニメ調トゥーン＋リム。アウトラインは未） |
| 8 | 大気フォグ → コーンメッシュ・ビーム(B) | 未着手 |

**スコープから外したもの**

- モーフターゲット（表情・リップシンク、7.1 / Phase 14）— 別途判断する
