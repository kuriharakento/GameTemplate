# Particle test results

This directory contains files generated by the particle integration,
serialization, benchmark, D3D12 validation, and lifecycle stability tests.

The generated `.json`, `.txt`, `.csv`, and log files may be deleted. They are
recreated by the corresponding test. Hand-authored particle test assets belong
in `application/Resources/json/particles/tests/`.
