# KCE 名前空間移行計画

## 1. 目的

`KCE` は `KentoCompoEngine` の略称とする。

外部ライブラリやapplication側の識別子との名前競合を防ぐため、`engine` が所有する既存の型、関数、定数、変数、名前空間を `KCE` 名前空間の配下へ移す。

```cpp
// 移行前
class Framework;
ecs::Registry registry;

// 移行後
namespace KCE
{
class Framework;
}

KCE::ecs::Registry registry;
```

## 2. 最上位の制約

今回行うのは、既存コードへの `KCE` 名前空間の追加と、それに必要な参照修正だけである。

次の変更は禁止する。

- 新機能の追加
- 既存機能の削除
- アルゴリズムや処理順序の変更
- クラスや関数の責務変更
- APIの再設計
- 名前空間以外の命名変更
- ファイルやディレクトリの移動
- 定数値、初期値、引数、戻り値の変更
- 互換ヘッダー、互換エイリアス、ラッパーAPIの追加
- ビルドエラーを隠すための `using namespace KCE;` の追加
- 今回の移行と無関係なリファクタリングや整形

コンパイルエラーやリンクエラーへの修正は、名前空間修飾、前方宣言、マクロ内の型名など、名前空間移行によって直接必要になった箇所だけに限定する。

## 3. 対象範囲

### 対象

- `engine` 配下でエンジンが所有する `.h`、`.hpp`、`.cpp`
- エンジンが所有する型、関数、定数、変数
- エンジンが所有する既存名前空間
- エンジン型の前方宣言
- シーンやコンポーネントなどの登録マクロ内にあるエンジン型名
- `application` 配下にあるエンジンAPIの参照

### 対象外

- `engine/externals` 配下の全ファイル
- 標準ライブラリと外部ライブラリが所有する型・関数・名前空間
- HLSLの組み込み型とシェーダーのエントリーポイント
- リソースパス、プロジェクト名、ディレクトリ名に含まれる `engine`
- `WinMain` など、外部仕様によってグローバルスコープが要求されるエントリーポイント
- applicationが独自に所有し、エンジン名前空間を拡張していない型・関数・名前空間

## 4. 移行規則

### 4.1 エンジン所有の宣言と定義

エンジンが所有する宣言と定義を `KCE` で囲む。

```cpp
#pragma once

#include <memory>

namespace KCE
{
class Framework
{
    // 既存の内容は変更しない
};
} // namespace KCE
```

実装ファイルも同じ名前空間へ移す。

```cpp
#include "Framework.h"

namespace KCE
{
void Framework::Initialize()
{
    // 既存の処理は変更しない
}
} // namespace KCE
```

### 4.2 既存名前空間

エンジンが所有する既存名前空間は、その名前を維持して `KCE` の子名前空間にする。

| 移行前 | 移行後 |
| --- | --- |
| `ecs` | `KCE::ecs` |
| `GameObjectComponent`（engine側） | `KCE::GameObjectComponent` |
| `Logger` | `KCE::Logger` |
| `MathUtils` | `KCE::MathUtils` |
| `collisionAlgorithm` | `KCE::collisionAlgorithm` |

### 4.3 インクルードと外部宣言

すべての `#include` は `namespace KCE` の外側に置く。

次のような外部所有の宣言・定義は `KCE` の内側へ移さない。

- `std`
- `DirectX`
- `Microsoft`
- Windows APIとCOM
- `ImGui`
- `Assimp`
- `nlohmann`
- `extern "C"`
- 外部APIがグローバルスコープを要求するコールバック
- 外部所有型に対する明示的特殊化

### 4.4 application側

application側では、エンジンAPIの参照だけを `KCE::` で完全修飾する。

```cpp
std::unique_ptr<KCE::Framework> game;
KCE::SceneManager::GetInstance();
KCE::ecs::Registry registry;
```

- ヘッダーに `using namespace KCE;` を記述しない。
- application独自型の名前や所属名前空間は変更しない。
- applicationが既存のエンジン名前空間を意図的に拡張している場合は、既存の所属関係を維持するため `KCE` の子名前空間として同じ場所へ移す。
- application側の所属について新しい設計判断は行わない。

## 5. 作業手順

### ステップ1: ベースライン確認

1. 親リポジトリと `engine` サブモジュールの差分を確認する。
2. ユーザーが指定したブランチを確認する。
3. `Debug|x64` と `Release|x64` の現状ビルド結果を記録する。
4. 現状ですでに存在する警告とエラーを記録する。

### ステップ2: 対象一覧の作成

コードはまだ変更せず、次を一覧化する。

- 変更対象ファイル
- エンジンが所有する既存名前空間
- 外部型の前方宣言
- `extern "C"` と外部コールバック
- テンプレートの明示的特殊化とインスタンス化
- 登録マクロと使用箇所
- application側から参照されるエンジン型

この一覧を基に、エンジン所有コードと外部所有コードの境界をCodexが確定する。

### ステップ3: engine側の名前空間化

エンジン側を依存関係に沿って変更する。ただし、途中でエンジンプロジェクト全体のビルド成功を必須としない。宣言を移動した時点で、未移行の参照元が一時的にコンパイルできなくなるためである。

作業単位ごとに、宣言とその直接の定義を同時に変更する。

1. 基本データ型、定数、ユーティリティ
2. base、time、input、audio
3. graphics、animation、light、line
4. gameobject、collision、ecs
5. scene、manager、framework

名前空間の追加以外に関数本体の変更がないことを、各作業単位の差分で確認する。

### ステップ4: application側の参照修正

engine側の完全修飾名が確定した後、application側の参照を修正する。

1. 型名と静的メンバーアクセス
2. 継承元
3. 関数の引数と戻り値
4. テンプレート引数
5. 登録マクロの引数
6. `ecs::` など既存子名前空間への参照

自由な一括置換は行わず、Codexが確定した変換表と対象ファイル一覧に基づいて変更する。

### ステップ5: コンパイル・リンク修正

エンジン全体を名前空間化し、application側の参照を修正した後にビルドする。

修正対象は次に限定する。

- 名前空間修飾の不足または重複
- 宣言と定義の名前空間不一致
- 前方宣言の名前空間不一致
- 登録マクロ内の型名
- テンプレート定義、特殊化、明示的インスタンス化の所属
- ADLで解決される既存関数や演算子の所属

ビルドを通すために処理ロジックを変更してはならない。

### ステップ6: 最終確認

1. `engine/externals` に差分がないことを確認する。
2. 名前空間移行と無関係な差分がないことを確認する。
3. `Debug|x64` と `Release|x64` をビルドする。
4. 起動、シーン遷移、描画、入力、オーディオ、衝突判定、ECS、デバッグUIを確認する。
5. 変更前後で既存機能の挙動が同じであることを確認する。

## 6. Codex・Gemini分担

### 6.1 Geminiの担当

Geminiには、判断を必要とせず、検索または指定済み変換表で検証できる作業だけを割り当てる。

#### G1: 読み取り専用の対象調査

- ステップ2の対象候補を検索する。
- 結果をファイル名と行番号付きで一覧化する。
- この段階ではソースコードを変更しない。

#### G2: Codex指定ファイルの定型修正

- Codexが事前に指定したファイルだけを編集する。
- 指定位置へ `namespace KCE` を追加する。
- 指定済みの変換表にある参照だけを完全修飾する。
- 関数本体、定数値、初期値、コメント、ファイル構成は変更しない。

対象候補は次のような低依存ファイルとするが、実際の担当ファイルはG1後にCodexが明示する。

- 単純な定数定義
- 単純な文字列ユーティリティ
- 単純な算術ユーティリティ
- 外部型や登録マクロを含まないデータ定義

#### G3: 指定済みのapplication参照修正

- Codexが作成した「移行前の完全名 → 移行後の完全名」の変換表を使用する。
- Codexが指定したapplicationファイルだけを変更する。
- 継承、テンプレート、マクロ、名前空間定義を含む箇所は担当しない。

#### G4: 読み取り専用の最終検索

- グローバル名前空間に残ったエンジン識別子候補を検索する。
- `engine/externals` の差分を確認する。
- ヘッダー内の `using namespace KCE;` を検索する。
- 結果を報告する。追加修正はCodexの指示があるまで行わない。

### 6.2 Codexの担当

Codexは、名前空間境界の判断と統合作業を担当する。

- G1の調査結果をレビューし、変更対象と除外対象を確定する。
- Geminiへ渡す具体的なファイル一覧と変換表を作る。
- DirectX、WRL、Windows API、COM、ImGui、Assimpなどの外部境界を処理する。
- 前方宣言、コールバック、テンプレート、ADLを処理する。
- SceneFactory、ComponentFactoryなどの登録マクロを処理する。
- GameObject、衝突判定、ECS、graphics、scene、frameworkを処理する。
- 継承、テンプレート、マクロを含むapplication側参照を処理する。
- Geminiの全差分をレビューする。
- ビルド、リンク、実行確認を行う。
- 名前空間移行以外の差分がないことを最終確認する。

### 6.3 作業順序

| 順番 | 担当 | 作業 | 完了条件 |
| --- | --- | --- | --- |
| 1 | Gemini | G1 読み取り専用調査 | 候補一覧を提出 |
| 2 | Codex | 対象確定・変換表作成 | 担当ファイルと変換表を確定 |
| 3 | Gemini | G2 指定ファイル修正 | 指定範囲だけの差分を提出 |
| 4 | Codex | engine複雑箇所の移行・G2レビュー | engine側の名前空間化完了 |
| 5 | Gemini | G3 指定application修正 | 指定範囲だけの差分を提出 |
| 6 | Codex | application複雑箇所・統合 | ソリューションがビルド可能 |
| 7 | Gemini | G4 読み取り専用検査 | 残存候補を報告 |
| 8 | Codex | 最終修正・検証 | 完了条件をすべて満たす |

同じファイルを同時に編集しない。ブランチ、コミット、統合方法はユーザーの指定に従う。

## 7. Geminiへの作業指示テンプレート

```text
KentoCompoEngine の既存コードを KCE 名前空間へ移行します。
新機能追加、設計変更、リファクタリングは行いません。

今回の担当は <G1/G2/G3/G4> だけです。

必須条件:
- engine/externals は変更しない。
- Codexが指定したファイル以外は変更しない。
- Codexが指定した変換表にない識別子は変更しない。
- ヘッダーに using namespace KCE; を追加しない。
- 関数本体、定数値、初期値、処理順序を変更しない。
- 前方宣言、テンプレート、マクロ、継承、外部API境界で判断が必要なら変更せず報告する。
- 完了時に変更ファイル、検証結果、未解決事項を報告する。
```

## 8. 検証項目

### 静的確認

- `engine/externals` に差分がない。
- エンジン固有の宣言と定義が `KCE` 配下にある。
- application側が旧グローバル名に依存していない。
- エンジンヘッダーとapplicationヘッダーに `using namespace KCE;` がない。
- `namespace KCE` の内側に標準・外部ヘッダーのインクルードがない。
- 外部所有の宣言が誤って `KCE` 配下へ移動していない。
- 前方宣言と実定義の完全修飾名が一致している。
- 関数本体、定数値、初期値、処理順序に意図しない差分がない。
- 新しい互換APIやラッパーが追加されていない。

### ビルド確認

- `KentoCompoEngine`: `Debug|x64`
- `KentoCompoEngine`: `Release|x64`
- ソリューション全体: `Debug|x64`
- ソリューション全体: `Release|x64`

ほかの構成はユーザーから指定された場合に確認する。

### 実行確認

- アプリケーションが起動・終了できる。
- 初期シーンとシーン切り替えが動作する。
- 2D、3D、ディファード、シャドウ描画が従来どおり動作する。
- 入力、オーディオ、時間管理が従来どおり動作する。
- GameObject、コンポーネント、ECSが従来どおり動作する。
- 衝突判定とデバッグUIが従来どおり動作する。

## 9. 完了条件

- 外部ライブラリを除くエンジン固有の宣言と定義が `KCE` 配下にある。
- application側のエンジン参照が正しい完全修飾名になっている。
- 旧グローバルAPIと互換エイリアスが残っていない。
- 対象ビルド構成が成功する。
- 名前空間移行以外の機能・設計・挙動変更がない。
- 主要機能が変更前と同様に動作する。
